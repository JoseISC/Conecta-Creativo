# AGENTS.md

## Cursor Cloud specific instructions

This repository ("Conecta-Creativo") is currently an empty/initial project with only a README.md.

### Current State
- No source code, frameworks, or dependencies are present.
- No package manager is configured (no `package.json`, `requirements.txt`, etc.).
- No services, databases, or external dependencies are required.
- No tests, lint checks, or build steps exist.

### For Future Agents
- When code is added to this repository, update this file with:
  - How to install dependencies
  - How to run the development server
  - How to run tests and lint
  - Any non-obvious configuration or startup caveats
- Check the README.md for project-level documentation as it evolves.
